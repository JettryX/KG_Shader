<?xml version="1.0" encoding="utf-8"?>
<project vert=" varying vec2 texCoord;&#xD; varying vec3 Normal;&#xD; varying vec3 Position;&#xD; &#xD; void main(void)&#xD; {&#xD;    gl_Position = gl_ProjectionMatrix * gl_ModelViewMatrix*gl_Vertex;&#xD;    Position = gl_Vertex.xyz;&#xD;    gl_TexCoord[0] = gl_TextureMatrix[0] * gl_MultiTexCoord0;&#xD;    texCoord = gl_TexCoord[0].xy;&#xD;    Normal = normalize(gl_Normal);&#xD; }" pix=" varying vec2 texCoord;&#xD; uniform sampler2D iTexture0;&#xD; varying vec3 Position;&#xD; uniform float iTime;&#xD; &#xD; void main(void)&#xD; {&#xD;     gl_FragColor = vec4(texture2D(iTexture0,texCoord).rgb,1.0);&#xD;     float x,y,time;&#xD;     float pi= 3.14159;&#xD;     int i;&#xD;     x= Position.x*10;&#xD;     y= Position.y*10; &#xD;      if(pow(x,3)+pow(y,2)&lt;1000) gl_FragColor = vec4(1,0,0,1);&#xD;      else gl_FragColor= vec4(0,0,0,0);&#xD;      if (pow((x-5),2)*4+4*pow(y+2,2)&lt;x+5) gl_FragColor= vec4(0+sin(iTime)/1,0+cos(iTime)/2,0+tan(iTime),1);&#xD;      if (pow((x-5),2)*4+4*pow(y-2,2)&lt;x+5) gl_FragColor= vec4(0+sin(iTime)/1,0+cos(iTime)/2,0+tan(iTime),1);&#xD;     if (sin(2*x*10)+cos(x*(y-15)*5)&lt;(x-7) &amp;&amp; (y-4)) gl_FragColor = vec4 (0,1,0,1);&#xD;     if (cos(y*2)*2*sin(iTime/370)&lt;x-10) gl_FragColor= vec4(0,1,1,0);&#xD;     if (pow(1*y,2)-4*x-1.5*x+30&gt;30) gl_FragColor= vec4(1,1,0,0);&#xD;     if (5*pow(x-2,5) -5*pow(y,2) &lt;y) gl_FragColor= vec4(0,0,0,0);&#xD;     if (10*pow(x-6.5,4) -2*pow(y,2) &lt;y) gl_FragColor= vec4(0,0,0,0);&#xD;       &#xD; }&#xD; " t0="D:\OpenGL\grafika_lab2-master\OpenGLrememberProj\texture3.bmp" t1="" t2="" t3="" />